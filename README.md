# VK Short Link Tool

Это учебный проект на Python, который позволяет сокращать ссылки через VK API и получать количество кликов по коротким ссылкам.  
Проект использует токен VK для взаимодействия с API.

## 🚀 Как запустить

### 1. Клонируйте репозиторий
```
git clone https://github.com/doingmov/API_VK
cd <имя папки репозитория>
```

2. Создайте виртуальное окружение

```
python -m venv venv
```

```
source venv/bin/activate  # Linux / macOS
```

```
venv\Scripts\activate     # Windows
```

3. Установите зависимости

```
pip install -r requirements.txt
```

4. Создайте файл `.env`

В корне проекта создайте файл с расширением `.env` и добавьте в него ваш VK токен:

```
API_VK=ваш_токен_VK
```

Файл `.env` должен находиться в той же папке, что и скрипт.

## ▶️ Как использовать

Запустите скрипт 

```
py main.py
```

Скрипт предложит ввести ссылку:

- Если вы введёте обычную ссылку, она будет сокращена через VK API.

- Если вы введёте уже короткую ссылку (vk.cc), скрипт покажет количество кликов по ней.

Пример работы:

```
Введите ссылку: https://example.com
https://vk.cc/c12345
```

```
Введите ссылку: https://vk.cc/c12345
42
```

## 🛠 Используемые технологии:

- Python 3

- requests

- python-dotenv

- VK API
